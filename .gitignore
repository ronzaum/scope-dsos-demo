# Private workspace — never committed
/private/

# Dependencies
node_modules/

# Trial day observations — never committed
/data/friday/trial_log.md

# Audit logs — never committed
api/logs/

# Generated report outputs — never committed
/data/outputs/

# Reference reports — visual layout calibration only, never committed
/data/reference-reports/

# Build artifacts
frontend/dist/

# Environment files
.env
frontend/.env
